'use strict';
const form=document.getElementById('loginForm'),button=document.getElementById('loginButton'),error=document.getElementById('loginError'),password=document.getElementById('password');
document.getElementById('showPassword').addEventListener('click',event=>{const visible=password.type==='text',toggle=event.currentTarget;password.type=visible?'password':'text';toggle.classList.toggle('visible',!visible);toggle.setAttribute('aria-label',visible?'显示密码':'隐藏密码');toggle.querySelector('span').textContent=visible?'显示':'隐藏'});
form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';button.disabled=true;button.querySelector('span').textContent='正在验证…';try{const response=await fetch('/api/auth/login',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:document.getElementById('username').value.trim(),password:password.value})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||'登录失败');location.replace('/')}catch(e){error.textContent=e.message;password.select()}finally{button.disabled=false;button.querySelector('span').textContent='安全登录'}});
fetch('/api/auth/session',{credentials:'same-origin'}).then(response=>{if(response.ok)location.replace('/')}).catch(()=>{});
